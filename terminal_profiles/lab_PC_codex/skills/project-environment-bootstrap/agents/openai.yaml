interface:
  display_name: "Project Environment Bootstrap"
  short_description: "Initialize local project environment checks"
  default_prompt: "Use $project-environment-bootstrap to initialize this project environment and local-only environment record."
