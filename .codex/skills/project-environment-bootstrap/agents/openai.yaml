interface:
  display_name: "Project Environment Bootstrap"
  short_description: "Verify and record project environment readiness"
  default_prompt: "Use $project-environment-bootstrap to verify this project environment and prepare a safe local-only environment record."
