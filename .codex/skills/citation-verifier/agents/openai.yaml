interface:
  display_name: "Citation Verifier"
  short_description: "Verify citation identity, metadata, and claim support"
  default_prompt: "Use $citation-verifier to verify these citations, their metadata, and the exact claims they support."
