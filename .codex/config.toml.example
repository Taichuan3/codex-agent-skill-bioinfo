[features]
multi_agent = true

[agents]
max_threads = 3
interrupt_message = true

# Keep model selection machine-local so Home/Lab/Server can choose a suitable
# cost, latency, and reasoning profile without changing the portable package.
# Do not add model keys here unless the installed Codex config reference and
# `codex doctor` confirm that the current client supports them.
