[agents]
enabled = true
max_concurrent_threads_per_session = 3
interrupt_message = true

# Keep model selection machine-local so Home/Lab/Server can choose a suitable
# cost, latency, and reasoning profile without changing the portable package.
# default_subagent_model = "..."
# default_subagent_reasoning_effort = "medium"
